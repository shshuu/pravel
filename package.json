{
  "name": "pravel-cloudflare-migration",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check public/js/app.js && node --check public/js/map.js && node --check public/js/youtube360.js && node --check src/worker.js"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
